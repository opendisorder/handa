version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    resource_class: medium
    steps:
      - run: echo "Hello from CircleCI!" && uname -a

workflows:
  build:
    jobs:
      - test
