name: gemini_live_fork
description: "Fork of gemini_live package modified for Vertex AI Gemini Live API with OAuth2 Bearer token auth and camelCase JSON serialization."
version: 2026.6.6
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  web_socket_channel: ^3.0.2
  http: ^1.3.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  json_serializable: ^6.9.4

flutter:
